program uendel;
   var uendel: boolean;
   var x: real;
   var luiz: real;
   var y: integer;
begin
    uendel := true;
    luiz := 1 + 2 - 3 / 45 + (8) * 9;
    uendel := 133 >= 5;
    if(3>=2) or (2+2<>4) then
        begin
            x := x + 1;
            x := x * x;
            y := 1.4 * 1;
            uendel := 1 and false;
        end
    else 
        x := 0;
end.
