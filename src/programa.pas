program test;
#declaracoes
	var sum, sub: integer;
	var activate: boolean;
	var vetor: array [ 1 .. 9] of integer;
#comando composto
	begin
		# comandos
		sum := 0;
		while i < 100 do
		vetor [7*4+5] := sub = sum;
		begin
			sum := sum + i;
			sub := ( sub - i ) * 7;
			if ( sub > sum ) then
				#something
				activate := true and untrue;
			else
				#something else
				activate := 1 > 2; # and 2 > 3
		end;

	end
.