program uendel;
    var uendel, luiz, neto, couto : integer;
    var x: array[1 .. 5] of integer; 
begin
    if 2 > 1 then
        begin
            x1[7*(4+5)] := sub = sum;
            y2 := a + a;
        end
    else
        begin
            x := x + 2 + 2 * 3 / 9;
            y :=  y * y;
            x1[7*(4+5)] := sub = sum;
        end
    ;
end.
