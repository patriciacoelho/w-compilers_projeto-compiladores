program uendel;
    var uendel : integer;
    var uendel: array[1 .. 5] of integer; 
begin
    if sub > sum then
        begin
            x1[7*(4+5)] := sub = sum;
            y2 := a + a;
        end
    else
        begin
            x := x + 2 + 2 * 3 / 9;
            y :=  y * y;
            x1[7*(4+5)] := sub = sum;
        end
    ;
end.
