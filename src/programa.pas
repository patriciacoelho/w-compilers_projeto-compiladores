program uendel; #testeç~
#declaraçes
    var x1 : array[true .. 12] of integer;
    var y2, uendel : real;

begin
    x := (123*2)/2;
    if (sub > sum) then
       x1[7*4+5] := sub = sum ;
       y2 := a = a;
end.
